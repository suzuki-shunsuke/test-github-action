---
name: Test
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v3
    - run: echo "FOO=BAR" >> "$GITHUB_ENV"
      shell: bash
    - run: echo "$FOO"
      shell: bash
