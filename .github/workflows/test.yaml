---
name: test
on: pull_request
jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - run: sleep 60
  foo:
    runs-on: ubuntu-latest
    if: |
      ! failure() && ! cancelled()
    needs:
      - setup
    steps:
      - run: echo hello
