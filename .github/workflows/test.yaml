---
name: Test
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
jobs:
  build:
    runs-on: windows-latest
    steps:
      - run: choco install ss-test-goreleaser-windows --version=0.1.0
      - run: hello
