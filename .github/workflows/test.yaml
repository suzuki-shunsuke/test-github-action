---
name: update pull request branch by pull request comment
on: issue_comment
jobs:
  update-branch:
    uses: suzuki-shunsuke/update-branch-workflow/.github/workflows/update-branch.yaml@feat/first-pr
    secrets:
      gh_app_id: ${{secrets.APP_ID}}
      gh_app_private_key: ${{secrets.APP_PRIVATE_KEY}}
