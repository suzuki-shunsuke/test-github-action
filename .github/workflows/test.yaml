---
name: test
on: pull_request
jobs:
  foo:
    timeout-minutes: 30
    runs-on: ubuntu-latest
    steps:
      - run: "true"
      - uses: suzuki-shunsuke/notify-workflow-error-action@feat-add-action
        if: always()
        with:
          issue_number: "225"
          status: ${{ job.status }}
