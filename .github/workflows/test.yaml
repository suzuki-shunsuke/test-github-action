---
name: test
on: pull_request
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'fo.!?%^@#$/ =bar' >> "$GITHUB_OUTPUT"
        id: output
      - run: echo "$VALUE"
        env:
          VALUE: ${{steps.output.outputs['fo.!?%^@#$/ ']}}
