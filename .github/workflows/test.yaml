---
name: test
on: pull_request
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "$ACTIONS_RESULTS_URL"
      - run: echo "$ACTIONS_RUNTIME_TOKEN"
      # - uses: actions/checkout@v3
      # - uses: actions/upload-artifact@v3
      #   with:
      #     path: README.md
