---
name: Test
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
jobs:
  foo:
    runs-on: ubuntu-latest
    if: "false"
    steps:
      - run: "true"
  bar:
    runs-on: ubuntu-latest
    needs: foo
    steps:
      - run: "true"
