---
name: test
on: pull_request
jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - run: gh -R "$GITHUB_REPOSITORY" pr ready 216
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
