---
name: test
on: pull_request
jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: golang:1.20
    steps:
      - uses: actions/checkout@83b7061638ee4956cf7545a6f7efe594e5ad0247 # v3.5.1
      - uses: aquaproj/aqua-installer@debug-461
        with:
          aqua_version: v2.9.0
        env:
          GITHUB_TOKEN: ${{github.token}}
