---
name: test
on: pull_request
jobs:
  foo:
    runs-on: ubuntu-latest
    steps:
      - run: sleep 60
  bar:
    needs: foo
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - run: exit 1
