---
name: Test
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
jobs:
  foo:
    runs-on: ubuntu-latest
    steps:
      - run: echo "::set-output name=name::foo"
        id: name
      - run: echo "$NAME"
        env:
          NAME: ${{steps.name.outputs.name}}
