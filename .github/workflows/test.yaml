---
name: Test
on:
  pull_request:
    branches: [main]
    types:
      - closed
jobs:
  foo:
    runs-on: ubuntu-latest
    steps:
      - run: env | grep GITHUB_
      - cat "$GITHUB_EVENT_PATH"
      - uses: actions/checkout@v3
