---
name: Test
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
jobs:
  foo:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::notice title=How to cancel the workflow::/cancel $GITHUB_RUN_ID"
      - run: sleep 30
