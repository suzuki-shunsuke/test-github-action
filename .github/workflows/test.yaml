---
name: test
on:
  pull_request: {}
  push:
    branches:
      - main
jobs:
  foo:
    runs-on: ubuntu-24.04
    permissions:
      pull-requests: write
    steps:
      - run: gh -R "$GITHUB_REPOSITORY" pr edit 239 --add-label yooo
        env:
          GITHUB_TOKEN: ${{ github.token }}
