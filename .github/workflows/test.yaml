---
name: test
on:
  pull_request: {}
  push:
    branches:
      - main
jobs:
  foo:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "$PATH"
      - run: echo "/workspace/bin" >> "$GITHUB_PATH"
      - run: echo "$PATH"
      - run: echo "/workspace/bin" >> "$GITHUB_PATH"
      - run: echo "$PATH"
