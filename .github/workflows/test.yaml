---
name: test
on: pull_request
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v6
        id: my-script
        with:
          script: |
            return context
      - run: "true"
        env:
          CONTEXT: ${{steps.my-script.outputs.result}}
