---
name: test
on: pull_request
jobs:
  foo:
    runs-on: ubuntu-latest
    steps:
      - run: exit 1
  bar:
    needs: foo
    runs-on: ubuntu-latest
    if: |
      ! failure() && ! cancelled()
    steps:
      - run: exit 0
