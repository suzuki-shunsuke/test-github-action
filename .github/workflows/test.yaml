---
name: Test
on: pull_request
jobs:
  foo:
    runs-on: ubuntu-latest
    steps:
      - run: "true"
  bar:
    runs-on: ubuntu-latest
    if: "false"
    steps:
      - run: echo bar
  zoo:
    runs-on: ubuntu-latest
    if: |
      always() && github.event.pull_request.user.login == 'suzuki-shunsuke'
    needs:
      - foo
      - bar
    steps:
      - run: echo hello
