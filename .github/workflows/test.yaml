---
name: Test
on: pull_request
jobs:
  foo:
    runs-on: ubuntu-latest
    if: failure()
    steps:
      - run: echo hello
  zoo:
    runs-on: ubuntu-latest
    if: success()
    needs:
      - foo
    steps:
      - run: echo hello
