---
name: watch
on:
  watch:
    types:
      - started
jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - run: gh -R "$GITHUB_REPOSITORY" issue comment 167 --body "$BODY"
        env:
          PAYLOAD: ${{toJSON(github)}}
          BODY: |
            [${{github.event.sender.url}}](${{github.event.sender.html_url}}) starred :star:
          GITHUB_TOKEN: ${{github.token}}
