---
name: test
on: [pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - id: echo-env
        name: echo env
        run: |
          echo "GITHUB_REF: $GITHUB_REF"
          echo "GITHUB_SHA: $GITHUB_SHA"
