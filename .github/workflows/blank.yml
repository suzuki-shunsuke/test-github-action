---
name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: aquaproj/aqua-installer@feat/check-aqua_version
      with:
        aqua_version: v0.9.0-0
        aqua_opts: ""
      env:
        AQUA_LOG_LEVEL: debug
