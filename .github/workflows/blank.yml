---
name: CI
on:
  pull_request:
    branches: [master]
  push:
    branches: [master]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: aquaproj/aqua-installer@v0.7.0
      with:
        aqua_version: v0.10.2
    - sleep 600
