---
name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: output
      id: output
      run: |
        echo "::set-output name=services::foo"
    - name: get output
      run: echo "${{ steps.output.outputs.services}}"
