---
name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: ["foo", "bar"]
    env:
      TFACTION_TARGET: ${{ matrix.target }}
    steps:
    - run: echo "$TFACTION_TARGET"
