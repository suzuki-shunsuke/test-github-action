---
name: test message
on:
  pull_request:
    branches: [master]
  push:
    branches: [master]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: echo '::debug title=title debug::debug'
    - run: echo '::notice title=title notice::notice'
    - run: echo '::warning title=title warning::warning'
    - run: echo '::error title=title error::error'
